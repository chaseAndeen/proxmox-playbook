---
- name: Authentik | Check for existing secret_key in SSM
  ansible.builtin.command: >
    aws ssm get-parameter
    --name "/infra/svc/authentik/secret_key"
    --profile "{{ aws_profile }}"
    --region "{{ aws_region }}"
  register: _authentik_secret_key
  ignore_errors: true
  changed_when: false
  no_log: true
  become: false
  delegate_to: localhost

- name: Authentik | Generate and store secret_key in SSM
  ansible.builtin.command: >
    aws ssm put-parameter
    --name "/infra/svc/authentik/secret_key"
    --type SecureString
    --value "{{ lookup('ansible.builtin.pipe', 'openssl rand -base64 60') | trim }}"
    --profile "{{ aws_profile }}"
    --region "{{ aws_region }}"
  no_log: true
  become: false
  delegate_to: localhost
  when: _authentik_secret_key is failed

- name: Authentik | Check for existing postgres_password in SSM
  ansible.builtin.command: >
    aws ssm get-parameter
    --name "/infra/svc/authentik/postgres_password"
    --profile "{{ aws_profile }}"
    --region "{{ aws_region }}"
  register: _authentik_postgres_password
  ignore_errors: true
  changed_when: false
  no_log: true
  become: false
  delegate_to: localhost

- name: Authentik | Generate and store postgres_password in SSM
  ansible.builtin.command: >
    aws ssm put-parameter
    --name "/infra/svc/authentik/postgres_password"
    --type SecureString
    --value "{{ lookup('ansible.builtin.pipe', 'openssl rand -base64 32') | trim }}"
    --profile "{{ aws_profile }}"
    --region "{{ aws_region }}"
  no_log: true
  become: false
  delegate_to: localhost
  when: _authentik_postgres_password is failed

- name: Authentik | Check for existing bootstrap_token in SSM
  ansible.builtin.command: >
    aws ssm get-parameter
    --name "/infra/svc/authentik/bootstrap_token"
    --profile "{{ aws_profile }}"
    --region "{{ aws_region }}"
  register: _authentik_bootstrap_token
  ignore_errors: true
  changed_when: false
  no_log: true
  become: false
  delegate_to: localhost

- name: Authentik | Generate and store bootstrap_token in SSM
  ansible.builtin.command: >
    aws ssm put-parameter
    --name "/infra/svc/authentik/bootstrap_token"
    --type SecureString
    --value "{{ lookup('ansible.builtin.pipe', 'openssl rand -hex 32') | trim }}"
    --profile "{{ aws_profile }}"
    --region "{{ aws_region }}"
  no_log: true
  become: false
  delegate_to: localhost
  when: _authentik_bootstrap_token is failed

- name: Authentik | Check for existing LDAP bind password in SSM
  ansible.builtin.command: >
    aws ssm get-parameter
    --name "/infra/svc/ldap/bind_password"
    --profile "{{ aws_profile }}"
    --region "{{ aws_region }}"
  register: _ldap_bind_password_check
  ignore_errors: true
  changed_when: false
  no_log: true
  become: false
  delegate_to: localhost

- name: Authentik | Generate and store LDAP bind password in SSM
  ansible.builtin.command: >
    aws ssm put-parameter
    --name "/infra/svc/ldap/bind_password"
    --type SecureString
    --value "{{ lookup('ansible.builtin.pipe', 'openssl rand -base64 32') | trim }}"
    --profile "{{ aws_profile }}"
    --region "{{ aws_region }}"
  no_log: true
  become: false
  delegate_to: localhost
  when: _ldap_bind_password_check is failed

- name: Authentik | Check for existing OIDC client secret in SSM
  ansible.builtin.command: >
    aws ssm get-parameter
    --name "/infra/svc/oidc/{{ item.name }}/client_secret"
    --profile "{{ aws_profile }}"
    --region "{{ aws_region }}"
  register: _oidc_secret_check
  loop: "{{ proxy_services_lan | selectattr('auth', 'equalto', 'oidc') | list }}"
  loop_control:
    label: "{{ item.name }}"
  ignore_errors: true
  changed_when: false
  no_log: true
  become: false
  delegate_to: localhost

- name: Authentik | Generate and store OIDC client secret in SSM
  ansible.builtin.command: >
    aws ssm put-parameter
    --name "/infra/svc/oidc/{{ item.item.name }}/client_secret"
    --type SecureString
    --value "{{ lookup('ansible.builtin.pipe', 'openssl rand -hex 32') | trim }}"
    --profile "{{ aws_profile }}"
    --region "{{ aws_region }}"
  loop: "{{ _oidc_secret_check.results }}"
  loop_control:
    label: "{{ item.item.name }}"
  when: item is failed
  no_log: true
  become: false
  delegate_to: localhost

- name: Authentik | Create compose directory
  ansible.builtin.file:
    path: "{{ svc_compose_dir }}/authentik"
    state: directory
    owner: root
    group: root
    mode: "0755"

- name: Authentik | Create blueprints directory
  ansible.builtin.file:
    path: "{{ svc_compose_dir }}/authentik/blueprints"
    state: directory
    owner: root
    group: root
    mode: "0755"

- name: Authentik | Remove legacy proxy-services.yml (wrong extension, not picked up by Authentik)
  ansible.builtin.file:
    path: "{{ svc_compose_dir }}/authentik/blueprints/proxy-services.yml"
    state: absent

- name: Authentik | Template proxy-services blueprint
  ansible.builtin.template:
    src: authentik/blueprints/proxy-services.yml.j2
    dest: "{{ svc_compose_dir }}/authentik/blueprints/proxy-services.yaml"
    owner: root
    group: root
    mode: "0644"
  notify: Restart authentik

- name: Authentik | Template amp-groups blueprint
  ansible.builtin.template:
    src: authentik/blueprints/amp-groups.yml.j2
    dest: "{{ svc_compose_dir }}/authentik/blueprints/amp-groups.yaml"
    owner: root
    group: root
    mode: "0644"
  notify: Restart authentik

# ─── OIDC Groups Scope Mapping ────────────────────────────────────────────────
# Ensure the 'groups' scope mapping exists for OAuth2 providers.
# This is required for AMP group-based role mapping to work.

- name: Authentik | Check if groups scope mapping exists
  ansible.builtin.uri:
    url: "https://{{ svc_authentik_domain }}/api/v3/propertymappings/provider/scope/?search=groups"
    headers:
      Authorization: "Bearer {{ authentik_bootstrap_token }}"
    status_code: 200
  register: _groups_scope_check
  delegate_to: localhost
  become: false

- name: Authentik | Create groups scope mapping
  ansible.builtin.uri:
    url: "https://{{ svc_authentik_domain }}/api/v3/propertymappings/provider/scope/"
    method: POST
    headers:
      Authorization: "Bearer {{ authentik_bootstrap_token }}"
    body_format: json
    body:
      name: "groups"
      scope_name: "groups"
      expression: |-
        claim = user.ak_groups.all().values_list('name', flat=True)
        return list(claim)
    status_code: [200, 201]
  register: _groups_scope_create
  delegate_to: localhost
  become: false
  when: _groups_scope_check.json.pagination.count | int == 0

- name: Authentik | Template LDAP blueprint
  ansible.builtin.template:
    src: authentik/blueprints/ldap.yaml.j2
    dest: "{{ svc_compose_dir }}/authentik/blueprints/ldap.yaml"
    owner: root
    group: root
    mode: "0644"
  notify: Restart authentik

- name: Authentik | Template environment file
  ansible.builtin.template:
    src: authentik/.env.j2
    dest: "{{ svc_compose_dir }}/authentik/.env"
    owner: root
    group: root
    mode: "0600"
  no_log: true
  notify: Restart authentik

- name: Authentik | Template docker-compose.yml
  ansible.builtin.template:
    src: authentik/docker-compose.yml.j2
    dest: "{{ svc_compose_dir }}/authentik/docker-compose.yml"
    owner: root
    group: root
    mode: "0644"
  notify: Restart authentik

- name: Authentik | Pull images
  ansible.builtin.command: docker compose pull
  args:
    chdir: "{{ svc_compose_dir }}/authentik"
  changed_when: false

- name: Authentik | Deploy stack
  ansible.builtin.command: docker compose up -d --remove-orphans
  args:
    chdir: "{{ svc_compose_dir }}/authentik"
  register: _authentik_up
  changed_when: "'Started' in _authentik_up.stdout or 'Recreated' in _authentik_up.stdout"

# Flush any pending restart before making API calls — ensures Authentik is running
# on the current config before we try to manage blueprints.
- name: Authentik | Flush handlers
  ansible.builtin.meta: flush_handlers

- name: Authentik | Wait for API to become ready
  ansible.builtin.uri:
    url: "https://{{ svc_authentik_domain }}/api/v3/core/users/me/"
    headers:
      Authorization: "Bearer {{ authentik_bootstrap_token }}"
    status_code: 200
  register: _authentik_api_ready
  retries: 30
  delay: 10
  until: _authentik_api_ready.status == 200
  delegate_to: localhost
  become: false

# ─── Blueprint lifecycle management ──────────────────────────────────────────
# Authentik's autodiscovery fires on worker restart but is subject to timing
# races that can silently drop the registration. We manage the blueprint
# instance directly via API so the playbook is the sole source of truth.

- name: Authentik | Check if proxy-services blueprint instance exists
  ansible.builtin.uri:
    url: "https://{{ svc_authentik_domain }}/api/v3/managed/blueprints/?search=proxy-services"
    headers:
      Authorization: "Bearer {{ authentik_bootstrap_token }}"
    status_code: 200
  register: _blueprint_list
  delegate_to: localhost
  become: false

- name: Authentik | Register proxy-services blueprint instance
  ansible.builtin.uri:
    url: "https://{{ svc_authentik_domain }}/api/v3/managed/blueprints/"
    method: POST
    headers:
      Authorization: "Bearer {{ authentik_bootstrap_token }}"
    body_format: json
    body:
      name: "proxy-services"
      path: "custom/proxy-services.yaml"
      enabled: true
      context: {}
    status_code: [200, 201]
  register: _blueprint_create
  delegate_to: localhost
  become: false
  when: _blueprint_list.json.pagination.count | int == 0

- name: Authentik | Get current blueprint file hash from available list
  ansible.builtin.uri:
    url: "https://{{ svc_authentik_domain }}/api/v3/managed/blueprints/available/"
    headers:
      Authorization: "Bearer {{ authentik_bootstrap_token }}"
    status_code: 200
  register: _blueprints_available
  delegate_to: localhost
  become: false

- name: Authentik | Set blueprint management facts
  ansible.builtin.set_fact:
    _blueprint_pk: >-
      {{
        (_blueprint_list.json.results | first).pk
        if (_blueprint_list.json.pagination.count | int) > 0
        else _blueprint_create.json.pk
      }}
    _blueprint_file_hash: >-
      {{
        (_blueprints_available.json
          | selectattr('path', 'equalto', 'custom/proxy-services.yaml')
          | list | first).hash
      }}
    _blueprint_applied_hash: >-
      {{
        (_blueprint_list.json.results | first).last_applied_hash
        if (_blueprint_list.json.pagination.count | int) > 0
        else ''
      }}

- name: Authentik | Apply blueprint if file changed or not yet applied
  ansible.builtin.uri:
    url: "https://{{ svc_authentik_domain }}/api/v3/managed/blueprints/{{ _blueprint_pk }}/apply/"
    method: POST
    headers:
      Authorization: "Bearer {{ authentik_bootstrap_token }}"
    status_code: 200
  register: _blueprint_apply
  failed_when: _blueprint_apply.json.status != 'successful'
  delegate_to: localhost
  become: false
  when: _blueprint_applied_hash != _blueprint_file_hash

# ─── LDAP blueprint lifecycle ─────────────────────────────────────────────────

- name: Authentik | Check if ldap blueprint instance exists
  ansible.builtin.uri:
    url: "https://{{ svc_authentik_domain }}/api/v3/managed/blueprints/?search=ldap"
    headers:
      Authorization: "Bearer {{ authentik_bootstrap_token }}"
    status_code: 200
  register: _ldap_blueprint_list
  delegate_to: localhost
  become: false

- name: Authentik | Register ldap blueprint instance
  ansible.builtin.uri:
    url: "https://{{ svc_authentik_domain }}/api/v3/managed/blueprints/"
    method: POST
    headers:
      Authorization: "Bearer {{ authentik_bootstrap_token }}"
    body_format: json
    body:
      name: "ldap"
      path: "custom/ldap.yaml"
      enabled: true
      context: {}
    status_code: [200, 201]
  register: _ldap_blueprint_create
  delegate_to: localhost
  become: false
  when: >
    _ldap_blueprint_list.json.pagination.count | int == 0 or
    (_ldap_blueprint_list.json.results | selectattr('name', 'equalto', 'ldap') | list | length) == 0

- name: Authentik | Set LDAP blueprint management facts
  ansible.builtin.set_fact:
    _ldap_blueprint_pk: >-
      {{
        (_ldap_blueprint_list.json.results | selectattr('name', 'equalto', 'ldap') | list | first).pk
        if (_ldap_blueprint_list.json.results | selectattr('name', 'equalto', 'ldap') | list | length) > 0
        else _ldap_blueprint_create.json.pk
      }}
    _ldap_blueprint_file_hash: >-
      {{
        (_blueprints_available.json
          | selectattr('path', 'equalto', 'custom/ldap.yaml')
          | list | first).hash
      }}
    _ldap_blueprint_applied_hash: >-
      {{
        (_ldap_blueprint_list.json.results | selectattr('name', 'equalto', 'ldap') | list | first).last_applied_hash
        if (_ldap_blueprint_list.json.results | selectattr('name', 'equalto', 'ldap') | list | length) > 0
        else ''
      }}

- name: Authentik | Apply LDAP blueprint if file changed or not yet applied
  ansible.builtin.uri:
    url: "https://{{ svc_authentik_domain }}/api/v3/managed/blueprints/{{ _ldap_blueprint_pk }}/apply/"
    method: POST
    headers:
      Authorization: "Bearer {{ authentik_bootstrap_token }}"
    status_code: 200
  register: _ldap_blueprint_apply
  failed_when: _ldap_blueprint_apply.json.status != 'successful'
  delegate_to: localhost
  become: false
  when: _ldap_blueprint_applied_hash != _ldap_blueprint_file_hash

# ─── amp-groups blueprint lifecycle ──────────────────────────────────────────

- name: Authentik | Check if amp-groups blueprint instance exists
  ansible.builtin.uri:
    url: "https://{{ svc_authentik_domain }}/api/v3/managed/blueprints/?search=amp-groups"
    headers:
      Authorization: "Bearer {{ authentik_bootstrap_token }}"
    status_code: 200
  register: _amp_groups_blueprint_list
  delegate_to: localhost
  become: false

- name: Authentik | Register amp-groups blueprint instance
  ansible.builtin.uri:
    url: "https://{{ svc_authentik_domain }}/api/v3/managed/blueprints/"
    method: POST
    headers:
      Authorization: "Bearer {{ authentik_bootstrap_token }}"
    body_format: json
    body:
      name: "amp-groups"
      path: "custom/amp-groups.yaml"
      enabled: true
      context: {}
    status_code: [200, 201]
  register: _amp_groups_blueprint_create
  delegate_to: localhost
  become: false
  when: >
    _amp_groups_blueprint_list.json.pagination.count | int == 0 or
    (_amp_groups_blueprint_list.json.results | selectattr('name', 'equalto', 'amp-groups') | list | length) == 0

- name: Authentik | Set amp-groups blueprint management facts
  ansible.builtin.set_fact:
    _amp_groups_blueprint_pk: >-
      {{
        (_amp_groups_blueprint_list.json.results | selectattr('name', 'equalto', 'amp-groups') | list | first).pk
        if (_amp_groups_blueprint_list.json.results | selectattr('name', 'equalto', 'amp-groups') | list | length) > 0
        else _amp_groups_blueprint_create.json.pk
      }}
    _amp_groups_blueprint_file_hash: >-
      {{
        (_blueprints_available.json
          | selectattr('path', 'equalto', 'custom/amp-groups.yaml')
          | list | first).hash
      }}
    _amp_groups_blueprint_applied_hash: >-
      {{
        (_amp_groups_blueprint_list.json.results | selectattr('name', 'equalto', 'amp-groups') | list | first).last_applied_hash
        if (_amp_groups_blueprint_list.json.results | selectattr('name', 'equalto', 'amp-groups') | list | length) > 0
        else ''
      }}

- name: Authentik | Apply amp-groups blueprint if file changed or not yet applied
  ansible.builtin.uri:
    url: "https://{{ svc_authentik_domain }}/api/v3/managed/blueprints/{{ _amp_groups_blueprint_pk }}/apply/"
    method: POST
    headers:
      Authorization: "Bearer {{ authentik_bootstrap_token }}"
    status_code: 200
  register: _amp_groups_blueprint_apply
  failed_when: _amp_groups_blueprint_apply.json.status != 'successful'
  delegate_to: localhost
  become: false
  when: _amp_groups_blueprint_applied_hash != _amp_groups_blueprint_file_hash

# ─── LDAP outpost token — sync Authentik → SSM → container ───────────────────
# Authentik is the authoritative source. We always fetch the current token and
# compare it against SSM. If they differ (first run, fresh Authentik install,
# or manual token rotation), SSM is updated and the container is restarted.
# No manual SSM cleanup is needed between environment rebuilds.

- name: Authentik | Fetch LDAP outpost token metadata
  ansible.builtin.uri:
    url: "https://{{ svc_authentik_domain }}/api/v3/core/tokens/?identifier=ak-outpost-truenas-ldap-outpost"
    headers:
      Authorization: "Bearer {{ authentik_bootstrap_token }}"
    status_code: 200
  register: _ldap_outpost_token_meta
  delegate_to: localhost
  become: false

- name: Authentik | Fetch LDAP outpost token key from Authentik
  ansible.builtin.uri:
    url: "https://{{ svc_authentik_domain }}/api/v3/core/tokens/{{ (_ldap_outpost_token_meta.json.results | first).pk }}/view_key/"
    method: POST
    headers:
      Authorization: "Bearer {{ authentik_bootstrap_token }}"
    status_code: 200
  register: _ldap_outpost_token_key
  no_log: true
  delegate_to: localhost
  become: false
  when: _ldap_outpost_token_meta.json.pagination.count | int > 0

- name: Authentik | Read current LDAP outpost token from SSM
  ansible.builtin.command: >
    aws ssm get-parameter
    --name "/infra/svc/ldap/outpost_token"
    --with-decryption
    --query "Parameter.Value"
    --output text
    --profile "{{ aws_profile }}"
    --region "{{ aws_region }}"
  register: _ldap_outpost_token_ssm
  ignore_errors: true
  changed_when: false
  no_log: true
  become: false
  delegate_to: localhost
  when: _ldap_outpost_token_key is not skipped

- name: Authentik | Update LDAP outpost token in SSM if changed
  ansible.builtin.command: >
    aws ssm put-parameter
    --name "/infra/svc/ldap/outpost_token"
    --type SecureString
    --value "{{ _ldap_outpost_token_key.json.key }}"
    --overwrite
    --profile "{{ aws_profile }}"
    --region "{{ aws_region }}"
  no_log: true
  become: false
  delegate_to: localhost
  register: _ldap_token_updated
  when:
    - _ldap_outpost_token_key is not skipped
    - _ldap_outpost_token_ssm is failed or
      _ldap_outpost_token_ssm.stdout | trim != _ldap_outpost_token_key.json.key

- name: Authentik | Re-template .env with updated LDAP outpost token
  ansible.builtin.template:
    src: authentik/.env.j2
    dest: "{{ svc_compose_dir }}/authentik/.env"
    owner: root
    group: root
    mode: "0600"
  no_log: true
  when: _ldap_token_updated is changed

- name: Authentik | Restart LDAP outpost container with updated token
  ansible.builtin.command: docker compose up -d --force-recreate authentik-ldap
  args:
    chdir: "{{ svc_compose_dir }}/authentik"
  when: _ldap_token_updated is changed
  changed_when: true

# ─── AMP groups blueprint lifecycle ────────────────────────────────────────────

- name: Authentik | Template AMP groups blueprint
  ansible.builtin.template:
    src: authentik/blueprints/amp-groups.yml.j2
    dest: "{{ svc_compose_dir }}/authentik/blueprints/amp-groups.yaml"
    owner: root
    group: root
    mode: "0644"
  notify: Restart authentik

- name: Authentik | Check if AMP groups blueprint instance exists
  ansible.builtin.uri:
    url: "https://{{ svc_authentik_domain }}/api/v3/managed/blueprints/?search=amp-groups"
    headers:
      Authorization: "Bearer {{ authentik_bootstrap_token }}"
    status_code: 200
  register: _amp_groups_blueprint_list
  delegate_to: localhost
  become: false

- name: Authentik | Register AMP groups blueprint instance
  ansible.builtin.uri:
    url: "https://{{ svc_authentik_domain }}/api/v3/managed/blueprints/"
    method: POST
    headers:
      Authorization: "Bearer {{ authentik_bootstrap_token }}"
    body_format: json
    body:
      name: "amp-groups"
      path: "custom/amp-groups.yaml"
      enabled: true
      context: {}
    status_code: [200, 201]
  register: _amp_groups_blueprint_create
  delegate_to: localhost
  become: false
  when: _amp_groups_blueprint_list.json.pagination.count | int == 0

- name: Authentik | Set AMP groups blueprint PK
  ansible.builtin.set_fact:
    _amp_groups_blueprint_pk: >-
      {{
        (_amp_groups_blueprint_list.json.results | selectattr('name', 'equalto', 'amp-groups') | list | first).pk
        if (_amp_groups_blueprint_list.json.results | selectattr('name', 'equalto', 'amp-groups') | list | length) > 0
        else _amp_groups_blueprint_create.json.pk
      }}

- name: Authentik | Get available blueprints hash
  ansible.builtin.uri:
    url: "https://{{ svc_authentik_domain }}/api/v3/managed/blueprints/available/"
    headers:
      Authorization: "Bearer {{ authentik_bootstrap_token }}"
    status_code: 200
  register: _amp_blueprints_available
  delegate_to: localhost
  become: false

- name: Authentik | Set AMP groups blueprint hash facts
  ansible.builtin.set_fact:
    _amp_groups_file_hash: >-
      {{
        (_amp_blueprints_available.json
          | selectattr('path', 'equalto', 'custom/amp-groups.yaml')
          | list | first).hash
      }}
    _amp_groups_applied_hash: >-
      {{
        (_amp_groups_blueprint_list.json.results | selectattr('name', 'equalto', 'amp-groups') | list | first).last_applied_hash
        if (_amp_groups_blueprint_list.json.results | selectattr('name', 'equalto', 'amp-groups') | list | length) > 0
        else ''
      }}

- name: Authentik | Apply AMP groups blueprint if changed
  ansible.builtin.uri:
    url: "https://{{ svc_authentik_domain }}/api/v3/managed/blueprints/{{ _amp_groups_blueprint_pk }}/apply/"
    method: POST
    headers:
      Authorization: "Bearer {{ authentik_bootstrap_token }}"
    status_code: 200
  register: _amp_groups_apply
  failed_when: _amp_groups_apply.json.status != 'successful'
  delegate_to: localhost
  become: false
  when: _amp_groups_applied_hash != _amp_groups_file_hash

# ─── Add amp_oidc_admins to AMP_Super Admins group ─────────────────────────────

- name: Authentik | Get AMP_Super Admins group PK
  ansible.builtin.uri:
    url: "https://{{ svc_authentik_domain }}/api/v3/core/groups/?name={{ amp_oidc_superadmin_group | urlencode }}"
    headers:
      Authorization: "Bearer {{ authentik_bootstrap_token }}"
    status_code: 200
  register: _amp_superadmin_group
  delegate_to: localhost
  become: false

- name: Authentik | Get PKs for amp_oidc_admins users
  ansible.builtin.uri:
    url: "https://{{ svc_authentik_domain }}/api/v3/core/users/?username={{ item }}"
    headers:
      Authorization: "Bearer {{ authentik_bootstrap_token }}"
    status_code: 200
  register: _amp_admin_users
  loop: "{{ amp_oidc_admins }}"
  delegate_to: localhost
  become: false

- name: Authentik | Add amp_oidc_admins to AMP_Super Admins group
  ansible.builtin.uri:
    url: "https://{{ svc_authentik_domain }}/api/v3/core/users/{{ item.json.results[0].pk }}/"
    method: PATCH
    headers:
      Authorization: "Bearer {{ authentik_bootstrap_token }}"
    body_format: json
    body:
      groups: "{{ item.json.results[0].groups + [_amp_superadmin_group.json.results[0].pk] }}"
    status_code: [200, 201]
  loop: "{{ _amp_admin_users.results }}"
  when:
    - (_amp_superadmin_group.json.pagination.count | int) > 0
    - (item.json.pagination.count | int) > 0
    - (_amp_superadmin_group.json.results[0].pk | string) not in (item.json.results[0].groups | map('string') | list)
  delegate_to: localhost
  become: false
